name: CI - Tests
on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests (placeholder)
        run: echo "Implement your test runner here" && exit 0
